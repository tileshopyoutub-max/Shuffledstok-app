export interface WatermarkSettings {
    enabled: boolean;
    text: string;
    opacity: number;
    fontSize: number;
    position: string;
    loopWatermark: boolean;

    paddingX: number;
    paddingY: number;
}